services:
  - type: web
    name: medibot
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --bind 0.0.0.0:${PORT:-10000} --workers 1 --timeout 300 --preload
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.8
